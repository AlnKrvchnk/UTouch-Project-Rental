import { styled } from '@mui/material/styles';

export const StyledFacilityDetailBox = styled('div')(({ theme }) => ({
    background: theme.palette.background.paper,
    padding: 20,
    border: '1px solid rgba(0, 0, 0, 0.1)',
}));
